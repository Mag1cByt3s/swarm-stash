# secrets — Discord OAuth credentials & session secret live here
.env

# runtime state: SQLite database + uploaded meme images
data/

# nix build output
result
result-*

# misc
node_modules/
nohup.out
*.log

# local Node runtime installed by setup.sh
.node/

# generated from public/app.ts at serve time — never commit
public/app.js
