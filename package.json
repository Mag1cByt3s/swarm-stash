{
  "name": "swarm-stash",
  "version": "1.0.0",
  "description": "Swarm Stash — trade Neuro-sama & Evil Neuro meme cards with the swarm",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "node server.ts",
    "check": "tsc -p tsconfig.json && tsc -p tsconfig.frontend.json"
  },
  "engines": {
    "node": ">=22.18"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "onFail": "download",
      "version": "11.10.0"
    }
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
